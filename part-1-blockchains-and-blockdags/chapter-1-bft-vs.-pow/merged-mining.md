# Merged Mining

_Merged mining_ is a technique that allows one PoW chain (typically a small chain) to piggyback on the efforts of another chain (typically a large chain, most commonly Bitcoin). This allows the same work to&#x20;
